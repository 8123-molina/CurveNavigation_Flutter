import 'package:flutter/material.dart';

class CadastroProdutosForm extends StatelessWidget {
  const CadastroProdutosForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cadastro de Contato'),
      ),
    );
  }
}
